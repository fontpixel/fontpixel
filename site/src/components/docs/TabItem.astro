---
/** Same API as Docusaurus's TabItem: value identifies this tab, label text comes from Tabs' values. */
interface Props {
  value: string;
  label?: string;
}
const { value, label } = Astro.props;
---

<section class="tabitem" data-tab-value={value} data-tab-label={label}>
  <slot />
</section>
