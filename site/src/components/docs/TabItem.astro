---
/** 与 Docusaurus TabItem 同 API：value 标识本页签，标签文字由 Tabs 的 values 提供。 */
interface Props {
  value: string;
  label?: string;
}
const { value, label } = Astro.props;
---

<section class="tabitem" data-tab-value={value} data-tab-label={label}>
  <slot />
</section>
