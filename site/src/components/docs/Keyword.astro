---
/** A "keyword value" entry line from the BDF spec (ported from the original site's Keyword.js). */
interface Props {
  keyword: string;
  value?: string;
}
const { keyword, value } = Astro.props;
---

<p><code><strong>{keyword}</strong> <i>{value}</i></code></p>
